# Web3Forms access key — server-side only (no VITE_ prefix).
# Read by the /api/contact serverless function. Set the same value in
# Vercel → Project → Settings → Environment Variables.
WEB3FORMS_ACCESS_KEY=
